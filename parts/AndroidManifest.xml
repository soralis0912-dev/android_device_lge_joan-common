<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2026 The LineageOS Project

     SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.lineageos.settings.joan"
    android:versionCode="1"
    android:versionName="1.0"
    android:sharedUserId="android.uid.system">

    <application
        android:label="@string/app_name">

        <!-- What the fuel gauge and LG's charging driver know about the cell
             that AOSP's battery screen has no field for. Six years in, the
             wear is the number somebody actually wants. -->
        <activity
            android:name=".BatteryHealthActivity"
            android:exported="false"
            android:label="@string/battery_health_title"
            android:theme="@style/Theme.SubSettingsBase">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.battery" />
            <meta-data
                android:name="com.android.settings.icon"
                android:resource="@drawable/ic_battery_health" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/battery_health_summary" />
        </activity>

    </application>
</manifest>
